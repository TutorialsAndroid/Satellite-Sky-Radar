<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <com.heatic.satelliteskyradar.SatelliteRadarView
        android:id="@+id/radarView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/tvNoSignal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="NO GPS SIGNAL\nGo outdoors"
        android:textColor="#AAFFFFFF"
        android:textSize="20sp"
        android:gravity="center"
        android:visibility="gone"
        android:background="#AA000000"
        android:padding="24dp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="16dp"
        android:orientation="vertical"
        android:background="#AA000000"
        android:padding="8dp">

        <TextView
            android:id="@+id/tvLocation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Location: waiting..."
            android:textColor="#FFFFFF"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/tvSatCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Satellites: 0"
            android:textColor="#FFFFFF"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/tvRawStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Raw data: waiting"
            android:textColor="#FFAAAA"
            android:textSize="12sp" />

        <!-- Privacy Policy button  -->
        <Button
            android:id="@+id/btnPrivacy"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="Privacy Policy"
            android:textSize="12sp"
            android:backgroundTint="#444444"
            android:textColor="#FFFFFF" />
    </LinearLayout>

</FrameLayout>